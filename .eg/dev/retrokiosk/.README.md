# retrokiosk

## Running the workload

```bash
eg compute local dev/retrokiosk
```

## What it does

Runs `flutter analyze` followed by `flutter test` against the console app. Despite the directory name, this workload is not kiosk-specific — it only exercises the console app's lint and test suite.
