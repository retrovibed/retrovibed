# lint

## Running the workload

```bash
eg compute local dev/lint
```

## What it does

Runs static analysis across the project in parallel: `flutter analyze` for the console app and `golangci-lint run ./...` for the Go backend.
