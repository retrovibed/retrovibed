# spike

## Running the workload

```bash
eg compute local dev/spike
```

## What it does

A scratch workload for verifying the source tarball build: it builds the retrovibed source tarball, then runs `ls -lha`, `file`, and `tar -tvf` against the resulting archive as diagnostics.
