# codegen

## Running the workload

```bash
eg compute local dev/codegen
```

## What it does

Regenerates all generated code for the project — Flutter/Dart scaffolding and codegen plus the native Go binding for the console app, and protobuf/Go codegen for the backend — then captures the resulting `git diff` as a patch and re-applies it to the workspace.
