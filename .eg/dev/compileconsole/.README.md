# compileconsole

## Running the workload

```bash
eg compute local dev/compileconsole
```

## What it does

Builds the console app from scratch for Linux: generates the Flutter project (pub get, launcher icons, config), generates the native Go binding (shared library + `ffigen`), and compiles a release Linux build via `flutter build linux --release`.
