# protogen

## Running the workload

```bash
eg compute local dev/protogen
```

## What it does

Regenerates the protobuf/gRPC bindings for both the console app (Dart) and the backend (Go) from `.proto/meta/*.proto`, then captures the resulting `git diff` as a patch and re-applies it to the workspace.
