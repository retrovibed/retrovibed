# flatpak

## Running the workload

```bash
eg compute local dev/flatpak
```

## What it does

Currently a disabled stub used to test the flatpak build — it logs "disabled" and fails intentionally. The commented-out body describes the intended pipeline once re-enabled: build console and shallows, install both, tar the `.dist/linux` output into a release tarball, and run `console.FlatpakBuild`.
