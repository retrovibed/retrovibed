# android release

```bash
gcloud auth application-default login --scopes="https://www.googleapis.com/auth/cloud-platform,https://www.googleapis.com/auth/androidpublisher"
eg compute local --gcp-auto release/android
```
