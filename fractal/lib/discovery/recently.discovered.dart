import 'package:flutter/material.dart';

class Discovered extends StatelessWidget {
  const Discovered({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(children: [Text("Recently Discovered")]);
  }
}
