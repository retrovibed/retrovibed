import 'package:flutter/material.dart';

class RecentlyWatched extends StatelessWidget {
  const RecentlyWatched({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(children: [Text("Recently Watched")]);
  }
}
